[package]
name = "etk-output"
description = "Cursor sinks — libei (Wayland-native) primary, /dev/uinput fallback"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
# scaffold — see PLAN.md §2.4
