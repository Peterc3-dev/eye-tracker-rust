[package]
name = "etk-config"
description = "Config data model + TOML loading (live-reload watcher wired in the daemon)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
# Config (de)serialisation — see PLAN.md §2.5.
# `notify` live-reload wiring lives in the daemon's event loop (TODO in lib.rs),
# so it is not pulled in here yet.
serde = { workspace = true }
toml = { workspace = true }
thiserror = { workspace = true }
