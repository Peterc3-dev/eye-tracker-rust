//! Phase 1 — live camera feed preview.
//!
//! Two side-by-side panes (left eye / right eye) with FPS + latency overlay.
//! Pass criteria: 60 fps preview, no tearing, ≤30 ms blink-to-pixel latency.
//!
//! See `PLAN.md` §6 Phase 1.

fn main() {
    println!("capture-preview scaffold — see PLAN.md §6 Phase 1");
}
